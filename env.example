# Copy to .env for local builds, or set these as environment variables in Cloudflare Pages (Settings → Environment variables).
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY=eyJ...   # the "anon public" key, safe to ship to browsers
SITE_URL=https://kitchenstudio.design   # used for sign-in redirects and share links
